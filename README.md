# Random
This is a simple demonstration that generates *n* unique random numbers of max size *l*. Each random number will be stored in an array *a*.

The way it works is each random number is generated with a range of (*( l / n )i* - *( l / n )( i + 1 ) - 1* ) where *i* is the index of the number.
